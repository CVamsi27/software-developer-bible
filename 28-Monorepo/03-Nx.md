---
section: Monorepo
category: Reference
tags: [concept, tool, reference]
---

# Nx

> Nx is a smart, fast, and extensible build system for monorepos. It provides tools for managing multiple projects, code generation, dependency analysis, and computation caching to improve developer experience and build performance.

## Definition

Nx is a build orchestrator and developer platform from Nrwl. Like Turborepo, it builds a dependency graph, parallelizes work, and caches results. Where it differs: rich plugin ecosystem with generators, executors, and a visual dependency graph. Strongest in large Angular / React / Node monorepos.

## Why It Matters (TL;DR)

- **Generators** — scaffold new apps, libs, components with one command
- **Affected commands** — only run tasks on projects that depend on a changed file
- **Computation cache** — local + remote (Nx Cloud) cache by content hash
- **Dependency graph** — visual representation of inter-project relationships
- **Plugin ecosystem** — first-class support for React, Angular, Node, Next.js, etc.

## How It Works

```text
┌─────────────────────────────────────────────────────────────────────┐
│                         NX ARCHITECTURE                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    nx.json (Workspace)                       │   │
│  │  • Project configuration                                    │   │
│  │  • Task runner configuration                                │   │
│  │  • Affected configuration                                   │   │
│  │  • Cache inputs/outputs                                    │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                      │
│                              ▼                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                   Dependency Graph                           │   │
│  │  • Static analysis of imports                              │   │
│  │  • Builds project.json graph (TypeScript Project References)│   │
│  │  • Visualized with `nx graph`                              │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                      │
│         ┌────────────────────┼────────────────────┐                │
│         ▼                    ▼                    ▼                │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐            │
│  │   Project   │    │   Project   │    │   Project   │            │
│  │     A       │    │     B       │    │     C       │            │
│  │  (cached)   │    │  (build)    │    │  (pending)  │            │
│  └─────────────┘    └─────────────┘    └─────────────┘            │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    Cache Layer                               │   │
│  │  • Local cache (node_modules/.cache/nx)                    │   │
│  │  • Nx Cloud remote cache (distributed)                     │   │
│  │  • Task pipeline caching                                    │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

## Nx vs Turborepo (2026)

| Dimension | Nx | Turborepo |
|-----------|-----|-----------|
| Project structure | `project.json` per project (or `package.json`) | `package.json` only |
| Generators | First-class (e.g., `nx g @nx/react:lib`) | None built-in |
| Dependency graph | Built-in, visual (`nx graph`) | Manual via `turbo ls` |
| Task definition | `project.json` targets with executors | `turbo.json` tasks |
| Affected | `nx affected:test --base=main` | `turbo run test --filter=...[main]` |
| Remote cache | Nx Cloud (free for OSS, paid for private) | Vercel remote cache (free with Vercel account) |
| Plugin ecosystem | Large (React, Angular, Node, Next.js, etc.) | Smaller; framework-agnostic |
| Learning curve | Steeper (more concepts) | Gentler |
| Best for | Large monorepos, Angular, code generation | Simple to medium monorepos, plain JS/TS |

## Code Examples

### 1. Workspace Setup (pnpm + Nx Preset)

```bash
# Create a new Nx workspace with pnpm
npx create-nx-workspace@latest myorg \
  --preset=react-monorepo \
  --packageManager=pnpm \
  --useGitHub

# Add a new React library
npx nx g @nx/react:lib packages/ui-button

# Add a new Node API
npx nx g @nx/node:app apps/api
```

```json
// nx.json (workspace config)
{
  "targetDefaults": {
    "build": {
      "cache": true,
      "dependsOn": ["^build"],
      "inputs": ["production", "^production"]
    },
    "test": {
      "cache": true,
      "inputs": ["default", "^production", "{workspaceRoot}/jest.preset.js"]
    },
    "lint": {
      "cache": true,
      "inputs": ["default", "{workspaceRoot}/.eslintrc.json"]
    }
  },
  "namedInputs": {
    "default": ["{projectRoot}/**/*", "sharedGlobals"],
    "production": [
      "default",
      "!{projectRoot}/**/?(*.)+(spec|test).[jt]s?(x)?(.snap)",
      "!{projectRoot}/tsconfig.spec.json"
    ],
    "sharedGlobals": ["{workspaceRoot}/tsconfig.base.json"]
  }
}
```

### 2. Project Configuration (`project.json`)

```json
// packages/ui-button/project.json
{
  "name": "ui-button",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/ui-button/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/ui-button"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/ui-button/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "storybook": {
      "executor": "@nx/storybook:storybook",
      "options": {
        "port": 4400
      }
    }
  },
  "tags": ["scope:ui", "type:lib"]
}
```

### 3. Affected Commands (CI Optimization)

```bash
# Run tests only for projects affected by changes since main
npx nx affected:test --base=main --head=HEAD

# Build, test, and lint affected projects
npx nx affected --target=build,test,lint --base=main

# Show what would be affected
npx nx affected:graph --base=main
```

### 4. Module Boundary Enforcement (Tags + ESLint)

```json
// nx.json
{
  "tags": {
    "scope:ui": [],
    "scope:utils": [],
    "scope:api": [],
    "type:feature": [],
    "type:ui": [],
    "type:util": []
  }
}
```

```typescript
// .eslintrc.json
{
  "@nx/enforce-module-boundaries": [
    "error",
    {
      "depConstraints": [
        { "sourceTag": "scope:ui",    "onlyDependOnLibsWithTags": ["scope:ui", "scope:utils", "type:util"] },
        { "sourceTag": "scope:utils", "onlyDependOnLibsWithTags": ["scope:utils", "type:util"] },
        { "sourceTag": "scope:api",   "onlyDependOnLibsWithTags": ["scope:api", "scope:utils", "type:util"] }
      ]
    }
  ]
}
```

This **prevents** a UI component from importing from a backend feature module — a common monorepo pitfall.

### 5. Generator Example (Custom Generator)

```typescript
// tools/generators/library/index.ts
import { Tree, formatFiles, generateFiles, names } from '@nx/devkit';

interface Schema {
  name: string;
  directory: string;
}

export default async function (tree: Tree, schema: Schema) {
  const { fileName, className } = names(schema.name);
  generateFiles(tree, __dirname, `${schema.directory}/${fileName}`, {
    fileName,
    className,
    tmpl: '',
  });
  await formatFiles(tree);
}
```

```bash
# Use the generator
npx nx g @myorg/tools:library button --directory=packages/ui
```

### 6. Visualizing the Dependency Graph

```bash
# Open interactive graph in browser
npx nx graph

# Show only affected projects
npx nx affected:graph --base=main

# Headless graph (CI artifact)
npx nx graph --file=graph.json
```

## Real-World Use Case

```text
Enterprise Monorepo (50+ projects, 5 teams):
┌─────────────────────────────────────────────────────────────────┐
│  Apps (5): web, admin, mobile, api, worker                     │
│  Libs (40+): feature-*, ui-*, data-*, infra-*                  │
│  Tools (3): eslint-config, tsconfig, storybook                 │
│                                                                 │
│  Build time before Nx:           12 minutes                    │
│  Build time after Nx:            45 seconds (full cache)      │
│  Build time, affected only:      8 seconds                     │
│  Cache hit rate (CI):            92%                           │
└─────────────────────────────────────────────────────────────────┘
```

## Common Mistakes

| Mistake | Fix |
|---------|-----|
| No `project.json` for non-standard projects | Add a minimal `project.json` so Nx can run tasks against it |
| Manual dependency graph maintenance | Use `nx graph` and `nx project-graph` to inspect; let Nx infer from imports |
| Skipping module boundary enforcement | Add `depConstraints` in ESLint to prevent cross-module imports |
| Building the whole monorepo on every PR | Use `nx affected` in CI — only build changed projects + their dependents |
| Not using generators | Generators enforce structure and save hours per new project |

## Best Practices

1. **Use `nx affected` in CI** — only build/test/lint what changed
2. **Enforce module boundaries with tags** — prevent accidental cross-module coupling
3. **Use generators for new projects** — consistent structure, less boilerplate
4. **Cache the lockfile inputs** — declare it in `namedInputs` for stable cache keys
5. **Distribute tasks with Nx Cloud** — even further speedup via task splitting across CI agents
6. **Run `nx dep-graph --watch` in dev** — visualize the impact of your changes
7. **Set `cache: true` only for deterministic tasks** — never cache deploys or migrations

## Performance Considerations

```text
Cache Hit Rate Optimization:
┌─────────────────────────────────────────────────────────────────┐
│  Affected-only builds:                                           │
│  • Default: only projects that depend on changed files run       │
│  • Use `nx affected --target=build` to scope the build          │
│                                                                 │
│  Local cache (per-developer):                                    │
│  • node_modules/.cache/nx                                       │
│  • Cleared when tool versions change                            │
│                                                                 │
│  Remote cache (Nx Cloud):                                       │
│  • Free for OSS, paid for private                               │
│  • Shared across team + CI                                      │
│  • Distributed task execution across CI agents                 │
│                                                                 │
│  Inputs hygiene:                                                 │
│  • Avoid `**/*` in inputs (file timestamps break cache)        │
│  • Use named inputs to exclude test files, configs, etc.        │
└─────────────────────────────────────────────────────────────────┘
```

## Summary

- Nx is a build orchestrator and developer platform with first-class generators and a dependency graph
- Strongest in large monorepos with multiple teams and strict module boundaries
- Use `nx affected` in CI to only build changed projects
- Module boundary enforcement via tags + ESLint prevents accidental coupling
- Nx Cloud provides remote cache and distributed task execution

---

## Cheat Sheet

```text
NX CHEAT SHEET
═══════════════════════════════════════════════════════════════

KEY COMMANDS:
  nx build <project>             # one project
  nx run-many --target=build     # all projects
  nx affected --target=test      # changed since base
  nx graph                       # interactive dependency graph
  nx g @nx/react:lib my-lib      # generate a lib
  nx reset                       # clear local cache

TARGET TYPES:
  executor  → runs a build/test/lint tool
  generator → scaffolds files
  run-commands → runs shell commands

INTERVIEW ANSWER:
  1. What Nx adds over Turborepo (generators, graph, tags)
  2. Module boundary enforcement via depConstraints
  3. Affected commands in CI
  4. When to choose Nx over Turborepo (large orgs, generators)
```

---

## See Also

- [Build Tools](../23-Build-Tools/)
- [CI/CD](../15-CI-CD/)
- [Git Advanced](../24-Git-Advanced/)
- [Monorepo Overview](01-Monorepo-Overview.md)
- [Monorepo Tools Deep-Dive](05-Monorepo-Tools.md)
- [Turborepo](02-Turborepo.md)


## References & Learn More

- [Affected Commands](https://nx.dev/ci/affected)
- [Module Boundaries](https://nx.dev/concepts/decisions/project-size)
- [Nx Cloud](https://nx.app/)
- [Nx Documentation](https://nx.dev/)
- [Nx Generators](https://nx.dev/extending-nx/recipes/local-generators)
- [Nx GitHub](https://github.com/nrwl/nx)
